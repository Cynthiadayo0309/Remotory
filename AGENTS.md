# AGENTS.md — Remotory

このリポジトリでは、以下の方針を必ず守ること。

## 1. プロダクトの目的

Remotory は「求人サイト」ではない。
日本に拠点があり、フルリモートで働けるポジションを持つ企業を、勤務条件・募集状況・最終確認日とともに探せる企業ディレクトリである。

ユーザーの基本導線は以下。

1. 企業を探す
2. フルリモート条件を確認する
3. 出社条件・勤務地域・募集状況を見る
4. 情報の最終確認日・根拠を見る
5. 公式採用サイトへ移動する

## 2. MVPでやらないこと

以下は実装しない。

- 一般ユーザー向けログイン
- Better Auth
- お気に入り
- 保存検索
- メール通知
- Stripe課金
- 閲覧履歴
- 口コミ・レビュー
- ユーザー投稿
- 企業からの掲載申請
- 企業自身による編集
- 求人詳細の保持
- 給与情報
- 技術スタック
- 福利厚生詳細
- 人気ランキング
- おすすめランキング
- 広告
- ダークモード
- スマホアプリ

将来拡張を見据えた設計は許可するが、MVPに勝手に追加しないこと。

## 3. UI

- HeroUI v3 を参考にする
- ライトモードのみ
- 白〜薄いグレー中心
- 余白を広めにする
- 過度な装飾を避ける
- ステータスだけに色を使う
- Chipを乱用しない
- 「公式採用サイトを見る」を最重要CTAとする
- アクセシビリティを重視する
- PC優先だがレスポンシブ対応する

HeroUI、Next.js、Cloudflare等のAPIやセットアップ方法は変更される可能性があるため、実装前に必ず最新の公式ドキュメントを確認すること。古い記憶だけでバージョンやAPIを決めないこと。

## 4. コード設計

巨大なファイルを作らない。
最低限、責務を以下のように分離する。

- UI Components
- Features
- DB access
- Fetch / crawl
- AI analysis
- Admin
- Types / validation
- Tests

外部ページ取得、AI判定、D1操作を密結合にしない。

## 5. AI更新ルール

AIは公開情報を直接書き換えてはいけない。

原則:

取得
→ 変更判定
→ 必要時のみAI解析
→ 変更候補作成
→ 管理者レビュー
→ 承認後に公開情報へ反映

「変更なし」の場合だけ、最終確認日を自動更新してよい。

## 6. セキュリティ

- `/admin/*` は必ず認証する
- 管理APIもサーバー側で認証する
- SQLはバインド変数を利用する
- SecretsをGit管理しない
- SSRF対策を入れる
- `http` / `https` 以外のURLを拒否
- localhost / private IP 等への取得を防ぐ
- リダイレクト先も検証する
- 外部HTMLをそのまま描画しない
- Zod等で入力値を検証する

## 7. 実装姿勢

- 不明な仕様を勝手に埋めない
- 実装前に既存ドキュメントを読む
- 変更する場合は影響範囲を確認する
- 小さな単位で実装・テストする
- 既存仕様と矛盾する追加機能を作らない
